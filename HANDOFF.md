# HANDOFF — 범표원두 원두추천 프로젝트

마지막 업데이트: 2026-09-10 (6일차 세션 끝)
작업 폴더: `C:\Users\bebeb\OneDrive - 범표원두\★.김현정\claude_2026`
git 브랜치: `update-settings` (마지막 커밋 `70c1c00`, 작업 트리 깨끗함. push 안 됨. `03_수서점상권분석/`은 다른 세션 것 — untracked로 두기)
실서비스: **https://beompyo-wondu-chucheon.vercel.app** (`70c1c00`까지 배포 완료)

---

## 0. 시작 전 체크리스트

- [ ] **같은 폴더를 다른 Claude 세션이 열고 있는지 확인.** 9/5에 두 세션이 `tools/build_products.py`를 동시에 고쳐 한쪽이 덮어써진 사고가 있었음. 포트 5500에 다른 세션 서버가 떠 있으면 그 신호. 검증용 서버는 **5501** (`.claude/launch.json`의 `원두추천-5501` 설정 사용).
- [ ] `git status` 확인 (마지막 세션 종료 시 깨끗했음).
- [ ] 설계 문서(`02_원두추천/docs/`)는 1일차 아이디어라 "가상 제품 30종" 기준. **이 HANDOFF.md 가 최신 기준.**

---

## 1. 이 프로젝트가 뭐냐면

고객이 채팅처럼 5~6개 질문에 답하면 **범표원두 스마트스토어에 실제로 있는 상품**을 하나 추천하고, 왜 추천했는지 쉬운 말로 설명하고, 비슷한 상황의 실제 구매자 후기를 보여준 뒤 구매 페이지로 연결하는 웹서비스. 결과 끝에서 친구에게 링크를 공유할 수 있음.

- 9/5에 "고객이 실제로 써서 구매까지 이어지는 것"으로 방향 확정 (사용자 지시)
- **규칙 기반**(선택지 클릭). AI 대화 아님. 오프라인 동작, 없는 상품을 지어낼 위험 없음
- 순수 HTML/CSS/JS, 외부 라이브러리 없음, `index.html` 더블클릭으로도 동작
- 톤: 짙은 브라운 `#2c1e14` 배경 / 크림 `#f3e3d0` 글자 / 크레마 브라운 `#c98a4b` 포인트

---

## 2. 지금까지 한 일 (시간순)

### 9/3~9/4 — 인사 페이지 + 프로토타입 v1
- `01_인사페이지/` 완료·배포 → https://kimhyunjeong-yangyang.vercel.app
- `02_원두추천/` v1: 가상 제품으로 대화 흐름·채팅 UI·추천 로직 검증

### 9/5 — 실제 판매 상품 51개로 재구축
- 스마트스토어 상품 CSV → 커피 상품 51개 연동, `data/tags.json`에 상품별 분류 기록
- 드립백 4종 세트에 **맛 선택 칩**(조선호랑이/호랑이형님/역삼동호랑이/디카페인호랑이)
- 시음샘플이 "오늘의 추천"을 차지하던 버그 수정, 답변마다 맞장구(`ack`) 추가
- Vercel 배포(팀 `bumpyobeans`, 프로젝트 `beompyo-wondu-chucheon`), `Bash(vercel *)` 허용 규칙

### 9/6 새벽 — 구매 전환 강화 (4일차)
- **원두 용량 질문** `q_amount` (핸드드립 선택자만): 100g/200g/500g/1kg. 상품명에서 용량을 읽어 필터
- **실제 후기 연동**: 리뷰 엑셀 2,032건 → `reviews.js` 273건(4점 이상, 상품당 최대 8건). 사진은 네이버 CDN hotlink, 60×60 썸네일
- 후기 있는 상품 +1점 가점, 오픈그래프 메타 + `og-image.jpg`
- 카드 순서: 이유 → 맛옵션 → 후기 → 구매 버튼

### 9/6 오후 — 시나리오 점검 + 문구·UX 개선 (5일차, 이번 세션)
1. **15개 시나리오 코드 시뮬레이션**(`tools/sim_scenarios.js`) + 브라우저 확인으로 어색한 곳 수정 (`e7717c1`):
   - 디카페인 조건이 **맛 옵션(디카페인호랑이 칩)** 을 인정하도록 → 선물/여행캠핑+디카페인이 fallback 이 아니라 드립백 4종(칩 기본선택)으로 정상 추천
   - 후보 0개면 곧바로 대표상품(fallback)으로 가지 않고 **상황(q1)·용량만 풀고 디카페인·신맛회피는 유지**(`relaxedQ1`)한 뒤 "딱 맞는 상품이 아직 없어 조건을 조금 넓혀 골랐어요"라고 솔직하게 안내
   - 추천 이유 문장을 **실제로 적용된 조건만** 말하게(용량 일치할 때만 용량 문장, 디카페인 아니면 디카페인 문장 안 함 등)
   - 선물 시나리오는 질문·이유가 **"받으실 분"** 기준(`textGift`)
   - q5 끊긴 문장 수정, "200g는"→"200g은", 맛 옵션 없는 상품엔 "여러 맛 옵션" 문구 대신 "어떤 취향에도 무난"
2. **선택지 문구·이모지** (`198e6cb`): 부담 맛 → "신맛이 강한 건 피하고 싶어요 😝" 등 문장형, 카페인 "상관없어요" → **"카페인 충전이 필요해요 ⚡"**(긍정형, 맞장구도 변경), 모든 선택지 끝에 이모지
3. **결과 카드 섹션 헤더** 🎯오늘의 추천 / 🌱새로운 커피 경험 / 🍬처음이라 부담되시면 — 제목 15px 굵게 + 한 줄 설명(`sectionHeaderEl`). "이런 점이 새로워요"는 오늘의 추천과 **제품군(`LINE_DESC`) 또는 맛 차이를 직접 비교**해 설명
4. **친구 공유** `buildShareBlock`: 결과 맨 아래 "🔗 친구에게도 알려주세요" 카드. 휴대폰이면 `navigator.share`, PC면 클립보드 복사, 둘 다 안 되면 주소를 화면에 표시. 공유 문구에 방금 받은 추천 상품명이 들어감. 하단 버튼은 "🔁 다시 확인하기"(첫 화면으로)
5. **링크 미리보기 배너** `og-image.jpg` 를 사용자가 만든 "내 취향의 커피를 찾아드려요!" 이미지(바탕화면 `범표원두이미지.png`, 1733×907)를 1200×630으로 잘라 교체

모두 브라우저(5501 로컬)에서 끝까지 돌려 확인, 콘솔 에러 0, 배포·커밋 완료.

### 9/10 — 라떼 캔커피 흐름 + 시그니처 + 새 후기 + 썸네일 (6일차, 이번 세션)
사용자가 수제 캔커피 3종(범표라떼=시그니처·궁극의 고소함 / 두유라떼=유당불내증용 / 온아바라=배구선수 김온아 스토리) 도메인 컨텍스트를 줌. 커밋 `70c1c00`.
1. **"라떼·베리에이션" 선택 시** q2·q3 대신 두 질문: `q_milk`(일반 우유 / 유당불내증→두유 / 상관없음), `q_latte`(범표라떼 / 온아바라 / 시그니처로). 유당불내증이면 `q_latte` 건너뜀.
2. `recommendLatte(a)` 가 `canByFlavor()` 로 3종 중 골라 `recommend()` 첫 줄에서 위임. 두유→두유라떼(`13237777944`), 온아바라→`7144276521`, 그 외→범표라떼(`5188817159`). 샘플은 범표라떼 맛보기(`9942645030`).
3. `data/tags.json` 새 필드: `signature`(범표라떼, priority 3→5), `soyMilk`(두유라떼), `canFlavor`, `story`(온아바라). `build_products.py` 가 이 필드들을 `products.js` 로 넘기도록 수정. `scoreOf` 에 `signature` +2.
4. 이유 문장: 라떼 경로 전용 분기(시그니처 문구 / 온아바라 스토리 / "350ml 2샷·500ml 3샷, 용량 커지면 샷도 비례" 안내 / "캔커피는 전부 디카페인으로도 주문 가능" 안내). `buildNewReason` 도 라떼 전용 분기.
5. **새 범표라떼 후기 엑셀** `review_20260910_171909.xlsx`(범표라떼만 2,144건) 반영. `build_reviews.py` 의 `XLSX_PATH`(str) → `XLSX_PATHS`(list, 2개 파일 병합, 없는 경로는 경고 후 skip). `TAG_KEYWORDS` 확장(꼬소/덜달/소풍/두유 등). 범표라떼 후기 8건 갱신.
6. **썸네일 잘림 수정**: `.prod-img` 를 `object-fit: cover` → `contain`, 260px 정사각(`aspect-ratio: 1/1`)으로. 상품(800×800)이 안 잘리고 꽉 참. `.prod-img-small`(64px 후기 썸네일)은 그대로.
7. `nextStep()` 을 `shouldSkip(key)` 헬퍼 + `while` 루프로 재작성(조건부 건너뛰기가 연속될 수 있어서).

브라우저(5501)에서 우유·두유 두 갈래 끝까지 확인, 콘솔 에러 0, 배포·커밋 완료.

---

## 3. 코드가 어떻게 돌아가나 (app.js, 약 1,100줄)

### 질문 흐름
```
q1 → q_amount(q1="핸드드립") → q_milk·q_latte(q1="라떼") → q2 → q3 → q4 → q5(q2="모름") → 결과
```
`FLOW = ["q1","q_amount","q_milk","q_latte","q2","q3","q4","q5","result"]`. 건너뛰기는 `nextStep()` 안의 `shouldSkip(key)` + `while` 루프. 규칙: `q_amount`는 핸드드립만, `q_milk`는 라떼만, `q_latte`는 라떼 & q_milk≠두유, `q2`·`q3`는 라떼면 건너뜀, `q5`는 q2="모름"만. 선택지 필드: `label`(버튼 글자, 이모지 포함) / `value`(내부 값) / `phrase`(이유 문장용) / `ack`(맞장구). 질문에 `textGift` 가 있으면 q1이 "선물"일 때 그 문구를 씀(q2/q3/q5).

### 추천 파이프라인 (`recommend()`)
0. **q1="라떼"면 맨 먼저 `recommendLatte(a)` 로 위임** — `canByFlavor("범표라떼"/"두유라떼"/"온아바라"/"맛보기")` 로 상품을 직접 고름(아래 파이프라인 안 탐). q_milk="두유"→두유라떼, q_latte="온아바라"→온아바라, 그 외→범표라떼. 새 경험은 나머지 시그니처 하나, 샘플은 범표라떼 맛보기.
1. `baseCandidates()` — `exclude`·`sample`·품절 제외
2. `filterByQ1` — q1 값으로 `method`/`scene` 필터
3. `filterByAmount` — 용량 일치 상품이 있으면 그것만
4. `filterByDecaf` — `isDecafOk(p)`: `decaf:true` **또는 flavors 에 taste "디카페인" 이 있으면** 통과
5. `filterByAcidAvoid` — 신맛회피면 산뜻한산미/과실감 제외(`다양`은 유지)
6. 후보 0개면 → `relaxedPool = filterByAcidAvoid(filterByDecaf(base))` 로 재시도(`relaxedQ1=true`). 그것도 0개면 `fallback=true`(대표상품)
7. `sortByScore` 1등 = 오늘의 추천. 점수 = 맛 일치 +3(또는 다양 +1) + 쓴맛회피 보정 +1 + 후기 있으면 +1 + **`signature`면 +2** + `priority×0.5`
8. 새로운 커피 경험 = 남은 후보 중 ① 제품군 다른 것 → ② 맛 구성 다른 것 → ③ 2등. 없으면 단계적으로 넓힘
9. 처음이라 부담되시면 = `sample:true` 중 q1 통과하는 것(relaxed 상태에선 대개 없음)

### 이유 문장 (`buildMainReason(a, p, fallback, relaxedQ1)`)
실제로 적용된 조건만 말함. 선물이면 "받으실 분이/께는" 접두어. `hasFlavorOptions(p)`(상품명에 "N종"/"외 N종")가 참일 때만 "여러 맛이 옵션으로" 문장. 디카페인은 `p.decaf` → "디카페인 상품만", 칩만 있으면 "디카페인호랑이 맛을 골라 두었어요", 둘 다 아니면 "아직 없어요".
**q1="라떼"면 별도 분기로 early return** — 우유/두유·라떼종류 문구 + 시그니처(`p.signature`) 문구 + 온아바라(`p.story`) 스토리 + "350ml 2샷·500ml 3샷" 안내 + (q4=디카페인이면) "캔커피는 전부 디카페인으로도 주문 가능" 안내.
`buildNewReason` 은 `LINE_DESC`(제품군 설명) 또는 `tasteLabelOf` 로 오늘의 추천과 비교. 라떼면 `canFlavor` 별 전용 문구.

### 후기 매칭 (`pickReview()`)
고객 답을 태그로(`currentSituationTags`: q1값, 목표맛, 디카페인, 용량) → 그 상품 후기 중 겹침 최다 1건. 겹침 0이어도 첫 후기 표시("실제 구매자 후기"), 1 이상이면 "나와 비슷한 분의 후기". 후기 없는 상품은 카드 생략.

### 태그 어휘 — 문자열이 정확히 같아야 함
| 축 | 값 |
|---|---|
| q1 | `핸드드립` `아메리카노간편` `라떼` `캡슐` `콜드브루` `선물` `여행캠핑` |
| 맛 | `고소` `균형` `산뜻한산미` `과실감` `다양` |
| 카페인 | `디카페인` |
| 용량 | `"100"` `"200"` `"500"` `"1000"` (문자열, 그램) |
| q_milk | `우유` `두유` `상관없음` (두유만 리뷰 태그로도 씀) |
| q_latte | `범표라떼` `온아바라` `시그니처` (= tags.json 의 `canFlavor` 값과 매칭) |

새 태그를 추가하면 app.js 답변값 ↔ `tools/build_reviews.py` 의 `TAG_KEYWORDS` 를 같이 맞출 것. `data/tags.json` 의 캔커피 3종에 `canFlavor` 필드가 있어야 `recommendLatte` 가 상품을 찾음.

---

## 4. 파일 구조 (02_원두추천/)

| 파일 | 역할 | 직접 수정? |
|---|---|---|
| `index.html` | 화면 + CSS 전체 + OG 메타 | O |
| `app.js` | 질문·추천·화면 그리기 전부 (IIFE 하나, Node 에서 `require` 하면 `recommend`/`buildMainReason`/`buildNewReason` export) | O |
| `data/tags.json` | 상품 분류표. **사람이 관리하는 유일한 데이터** | O |
| `products.js` | 상품 51개 | **X** → `python tools\build_products.py` |
| `reviews.js` | 후기 273건 (엑셀 2개 병합 결과) | **X** → `python tools\build_reviews.py` |
| `og-image.jpg` | 링크 미리보기 배너 1200×630 | O (원본: 바탕화면 `범표원두이미지.png`) |
| `tools/sim_scenarios.js` | **19개 시나리오 추천·후기·문구를 한 번에 출력** (`node 02_원두추천/tools/sim_scenarios.js`) | O |
| `tools/build_*.py`, `fetch_images.py` | 데이터 재생성 | O |
| `.vercel/` | Vercel 연결 정보 (gitignore) | X |

원본 데이터(git 에 없음): 상품 `C:\Users\bebeb\Desktop\Product_20260905_150247.csv`, 후기 `review_20260906_003018.xlsx`(종합) + `review_20260910_171909.xlsx`(범표라떼 전용 2,144건). `build_reviews.py` 의 `XLSX_PATHS` 리스트에 둘 다 들어 있음.

---

## 5. 데이터 현황

- 상품 51개: 원두 11 / 캔커피 10 / 파우더 6 / 콜드브루 6 / 선물세트 6 / 드립백 5 / 캡슐 4 / 티백 3. 업소용 1개 `exclude`
- 후기 273건이 40개 상품에. **후기 없는 상품 11개**: 선물세트 5개 전부, 디카페인 계열 4개, 커피티백 취향 4종, 밸런스 원두 500g
  - 선물 시나리오는 후기 가점 덕에 **후기 8건 있는 `7061025713` 선물세트**가 오늘의 추천으로 나옴(후기 "선물받게 되었습니다"). 문제 없음
- 후기 태그 분포: 라떼 104, 고소 94, 아메리카노간편 52, 디카페인 48, 산뜻한산미 45, 캡슐 40, 선물 37, 핸드드립 30, 콜드브루 22, 200g 19, 균형 18, 500g 17, 여행캠핑 15, 1kg 11, 100g 10, **과실감 4** (9/6 기준, 9/10 병합 후 약간 달라짐)
- 취향별 원두(고소한/밸런스/산미있는/디카페인)는 **500g만** 있음. 100g/200g/1kg 은 `9종` 옵션 상품 → 200g 고르면 "9종 중 고르세요" 상품이 나오는 게 정상
- **수제 캔커피 3종**: 범표라떼 `5188817159`(29,000, signature, 후기 8), 두유라떼 `13237777944`(35,300, decaf 고정, 후기 3), 온아바라 `7144276521`(37,800, story, 후기 8). 350/500ml·디카페인은 상품 하나 안의 옵션 → 용량 질문은 안내용, 링크는 안 바뀜

---

## 6. 회귀 확인용 시나리오 (이번 세션 브라우저에서 확인한 결과)

| q1 | 용량 | q2 | q3 | q4 | 오늘의 추천 | 새로운 경험 | 후기 |
|---|---|---|---|---|---|---|---|
| 아메리카노간편 | — | 고소 | 신맛회피 | 충전 | 드립백 4종 7개입 9,900 (조선호랑이 칩) | 동결건조 파우더 4종 | 비슷한 분 |
| 핸드드립 | 200g | 고소 | 없음 | 충전 | 당일로스팅 원두 200g 9종 12,000 | 볼리비아 200g | 비슷한 분 |
| 핸드드립 | 100g | 산뜻 | 없음 | 충전 | 홈카페 원두 100g 9종 6,500 | 드립백 4종(역삼동호랑이 칩) | 2/3, 3/3 일치 |
| 선물 | — | 산뜻 | 없음 | **디카페인** | 드립백 4종(디카페인호랑이 칩), "조건 넓혀 골랐어요" | 두유라떼 디카페인 5캔 | "임신 중 디카페인… 친구가 선물" |
| 선물 | — | 고소 | 없음 | 충전 | 선물세트 드립백·원두·티백·콜드브루 26,500 | 콜드브루 선물세트 | 비슷한 분 |

라떼 경로(9/10 브라우저 확인):

| q_milk | q_latte | q4 | 오늘의 추천 | 새로운 경험 |
|---|---|---|---|---|
| 우유 | 범표라떼 | 충전 | 범표라떼 5캔 29,000 | 온아바라 5캔 |
| 두유(유당불내증) | (건너뜀) | 충전 | 두유라떼 5캔 32,000 | 범표라떼 5캔 |
| 우유 | 온아바라 | 충전 | 온아바라 5캔 (김온아 스토리) | 범표라떼 5캔 |
| 상관없음 | 시그니처 | 디카페인 | 범표라떼 5캔 (+디카페인 안내) | 온아바라 5캔 |

전체 19개는 `node 02_원두추천/tools/sim_scenarios.js` 로 30초 안에 다시 볼 수 있음(A~I, L1~L4). 기능을 고친 뒤엔 이 스크립트 먼저 돌리고, 위 표 일부는 브라우저로도 확인할 것.

---

## 7. 막힌 점 / 주의사항

1. **스마트스토어 우측 배너 미완료 (사용자 직접 작업).** 판매자센터 "노출관리 → 스마트스토어 → PC → 배너/우측배너 컴포넌트"에 이미지+링크 넣는 방식(정확한 메뉴명 미확인). 세로로 좁은(폭 170px대) 배너 이미지 제작을 요청받을 수 있음. `og-image.jpg` 원본(바탕화면 `범표원두이미지.png`)을 재활용하면 톤이 맞음.
2. **카톡 링크 미리보기 캐시.** 같은 주소를 전에 보낸 적 있으면 옛 이미지가 보일 수 있음 → https://developers.kakao.com/tool/debugger/sharing 에서 주소 넣고 "초기화". 또 `localhost` 주소로 보내면 이미지가 절대 안 뜸(사용자가 8791 포트 로컬 서버로 테스트한 적 있음).
3. **브라우저 팬에서 `*.vercel.app` 열람이 정책상 막힘.** 배포 확인은 `curl` 로 app.js 내용을 받아 확인하는 방식(이번 세션에서 그렇게 함). 화면 확인은 5501 로컬 서버. 브라우저 팬이 숨겨져 있으면 클릭이 타임아웃 → `javascript_tool` 로 버튼 클릭해 진행하면 됨.
4. **Claude in Chrome 확장 미연결.** 판매자센터 같이 보려면 설치·로그인 필요. 로그인/비밀번호는 절대 대신 입력하지 않음.
5. **후기 사진 품질 들쭉날쭉.** 제외 기준 없음. 필요하면 `build_reviews.py`에 제외 리뷰번호 목록.
6. **선물세트 5종 후기 0건, 과실감 태그 4건뿐** — 코드로는 못 고침. 후기 엑셀 다시 받아 `build_reviews.py` 재실행해야 함.
7. `TAG_KEYWORDS`와 `data/tags.json`은 초안. `hasFlavorOptions` 는 상품명의 "N종/외 N종"으로 옵션 유무를 추정하므로, 옵션이 있는데 이름에 "종"이 없는 상품(예: 콜드브루 500ml "외")은 "무난한 상품" 문구가 나옴. 어색하면 tags.json 의 `short` 이름에 "N종"을 넣거나 정규식을 손보면 됨.
8. **두유라떼 상품(`13237777944`)이 데이터상 `decaf: true` 로 고정.** 이름도 "두유라떼 디카페인 5캔". 유당불내증만 있고 디카페인은 원치 않는 분에게도 이게 나옴. 실제 스토어 상품에 일반 두유라떼 옵션이 있으면 `tags.json` 에서 `short`·`decaf` 를 바꿔야 함(사용자 확인 대기 중).
9. git CRLF 경고는 무해함.

---

## 8. 하지 말 것

- `products.js`, `reviews.js` 직접 편집
- 포트 5500 사용
- 저장소 루트에서 `vercel` 실행 — **반드시 `cd 02_원두추천` 후**
- 후기·상품 지어내기. 없으면 생략
- `.env` 수정
- 배포·커밋을 미리 하기 (사용자가 "배포하고 커밋해줘"라고 할 때만)

---

## 9. 사용자와 일하는 방식 (확인된 것)

- 답은 **한국어**, 짧게, 핵심부터. 전문용어는 풀어서
- 기능 하나 만들기 전에 "무엇을 만들지" 한 줄로 먼저, 만든 뒤 **브라우저에서 직접 확인**한 후에만 "완료"
- 배포·커밋은 **매번 사용자가 말할 때만**
- 사용자는 실제 화면을 캡처해 보내며 피드백함(선택지 문구, 섹션 헤더, 카톡 공유 결과 등). 캡처에 빨간 체크가 있으면 그 부분이 대상
- 사용자 PC 바탕화면에 직접 만든 이미지가 있을 수 있음(`find` 로 최근 이미지 검색해서 찾았음)
- 코드 작성은 Sonnet 서브에이전트에 한 문단 사양으로 위임하고 결과를 직접 검증(이번 세션 3회 위임, 문제 없음). 3줄짜리 문구 수정은 직접 함
- 사용량 크레딧 걱정이 있음(프로모션 크레딧 9/19 만료). 서브에이전트·긴 탐색은 필요할 때만

---

## 10. 남은 일

1. **셀럽 방문 스토리·사진** (9/10 논의 중) — 전미도 배우(디카페인파 → 디카페인 범표라떼 재방문), 김온아·곽민정(절친, 함께 방문). 사용자가 매장 촬영 사진 2장 보냄(전미도 님 / 김온아·곽민정 님, 텍스트 오버레이 포함). **초상권·퍼블리시티권 확인 필요** — 인스타 스토리와 달리 상업 사이트에 이름+사진을 추천 근거로 쓰는 건 당사자/소속사 동의가 있어야 안전. 동의 확인 전까지 사진은 안 올림. 텍스트 스토리(온아바라 유래 등)는 이미 공개된 것이라 반영 가능.
2. **스마트스토어 우측 배너 이미지 제작 + 연결** (7-1)
3. **두유라떼 `decaf` 고정 문제** (7-8) — 실제 옵션 확인 후 tags.json 수정
4. Supabase 상담 데이터 저장 (설계서 6단계, 가입만 됨)
5. `data/tags.json` 분류 검토 + 7-7 옵션 유무 정리
6. 후기 엑셀 재수집(선물세트·과실감 보강)
7. `01_인사페이지/ocean.html` 최종 확인 (급하지 않음)

---

## 11. 다음 첫 단계

**셀럽 스토리를 어떻게 넣을지 사용자와 정한다.** 사진은 초상권 동의 여부부터 확인. 동의됐다면 디카페인 범표라떼 카드에 전미도 스토리+사진, 온아바라 카드에 김온아·곽민정 스토리+사진. 안 됐다면 텍스트만(사진 슬롯은 비워두고 나중에 이미지만 끼우게). 그다음 스마트스토어 우측 배너 이미지 제작.

---

## 12. 실행/재생성 방법

로컬 확인 (브라우저 팬은 `.claude/launch.json` 의 `원두추천-5501`):
```
python -m http.server 5501 --directory 02_원두추천
```

시나리오 19개 한 번에 점검:
```
node 02_원두추천/tools/sim_scenarios.js
```

데이터 갱신 (저장소 루트에서):
```
python 02_원두추천\tools\build_products.py
python 02_원두추천\tools\fetch_images.py
python 02_원두추천\tools\build_reviews.py
```

배포:
```
cd 02_원두추천
vercel --prod --yes
```
끝에 `readyState: READY` 가 나오면 성공. 확인: `curl -s https://beompyo-wondu-chucheon.vercel.app/app.js | grep 특정문자열`

커밋 메시지 끝에 `Co-Authored-By: Claude ... <noreply@anthropic.com>` 관례(세션마다 모델명 다름).

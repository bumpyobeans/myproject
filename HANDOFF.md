# HANDOFF — 범표원두 원두추천 프로젝트

마지막 업데이트: 2026-09-05
작업 폴더: `C:\Users\bebeb\OneDrive - 범표원두\★.김현정\claude_2026`

---

## ⚠️ 먼저 확인할 것 — 다른 세션이 같은 폴더에서 동시에 작업 중

`ListAgents` 로 확인한 결과, **`claude-2026-30`** 이라는 다른 인터랙티브 세션이 7분 전부터 켜져 있고, 이 문서를 쓰는 지금도 `02_원두추천/data/tags.json` 과 `tools/build_products.py` 가 내가 마지막으로 읽은 뒤 계속 바뀌고 있습니다 (아래 "지금까지 한 일 → 원두추천 v2"의 `flavors` 필드가 그 흔적).

**새 세션을 시작하기 전에 다른 창에서 이 프로젝트를 열어둔 게 있는지 먼저 확인하세요.** 두 세션이 같은 파일을 동시에 고치면 한쪽 작업이 덮어써질 수 있습니다. 있다면 그쪽을 마저 끝내고 오거나, 이 문서를 읽고 이어가기 전에 그 세션을 닫아주세요.

---

## 이 프로젝트가 뭐냐면

내일바꿈 양양 행사 발표용으로 시작했지만, **"발표에서 보여주기 좋은 것"이 아니라 "고객이 실제로 써서 구매까지 이어지는 것"** 으로 방향을 바꿨습니다 (2026-09-05, 사용자 지시). 고객이 몇 가지 질문에 답하면 **범표원두 스마트스토어에 실제로 있는 상품**을 추천하고, 구매 페이지로 바로 연결합니다.

설계 문서(`02_원두추천/docs/`)는 1일차 아이디어 단계 문서라 "가상 제품 30종" 기준으로 쓰여 있습니다. **지금은 그 문서보다 이 HANDOFF.md 가 최신 기준**입니다.

---

## 지금까지 한 일

### 1~2일차 (9/3~9/4): 인사 페이지 + 상담 프로토타입 v1
- `01_인사페이지/` : 커피·바다 컨셉 페이지 2종, Vercel 배포 완료 → https://kimhyunjeong-yangyang.vercel.app
- `02_원두추천/` 프로토타입 v1: **가상 제품 30개**(설계서 속 맛 이름)로 만든 상담 화면. 대화 흐름·채팅 UI·추천 로직 자체는 검증됨

### 3일차 (9/5): 방향 전환 — 실제 판매 상품으로 재구축
사용자가 스마트스토어 상품 CSV(`C:\Users\bebeb\Desktop\Product_20260905_150247.csv`, 전체 63개 상품)를 제공. 분석 결과:

- 프로토타입 v1이 추천하던 "조선호랑이 드립백" 같은 개별 상품은 **실제로 존재하지 않음** — 스토어는 `드립백 커피 4종 7개입`처럼 여러 맛을 **옵션**으로 묶어서 판매 중
- 판매중·전시중인 커피 관련 상품 **51개** 확인 (원두/드립백/티백/캡슐/파우더/콜드브루/캔커피/선물세트)
- 대표이미지 URL이 CSV에 있고 다운로드 가능함을 확인 (HTTP 200)
- 취향별 원두(`고소한 원두 500g 3종` 등)와 3,000원대 저가 샘플 상품이 이미 존재 → 추천 → 구매 전환에 결정적

**만든 것:**
- `02_원두추천/data/tags.json` — 51개 상품 각각에 대해 **내가 직접 판단**해서 붙인 분류표 (제품군/음용방식/상황/맛/디카페인/샘플여부/우선순위). 필드 설명이 파일 안에 `_설명`, `_필드` 키로 들어있음. **CSV가 바뀌어도 이 파일 구조는 유지하고 값만 다시 판단해야 함**
- `02_원두추천/tools/build_products.py` — CSV + tags.json → `products.js` 자동 생성 스크립트
- `02_원두추천/tools/fetch_images.py` — CSV의 대표이미지 URL 51개를 받아 `images/p_{상품번호}.jpg` 로 축소 저장 (평균 85KB, 최대 192KB)
- `02_원두추천/products.js` — 자동 생성됨, **직접 고치지 말 것**. `data/tags.json` 고친 뒤 `python tools/build_products.py` 재실행
- `02_원두추천/app.js` — 추천 점수 로직을 실제 상품 구조(`method`/`scene`/`taste`/`decaf`/`sample`/`priority`/`price`/`stock`/`url`)에 맞게 재작성
- 기존 가상 제품 4종 사진(조선호랑이 등 단독 컷)은 더 이상 안 쓰여서 삭제됨 (실제 상품 이미지로 교체)

**직접 브라우저에서 확인한 것 (로컬 서버 `http://localhost:5500`):**

| 시나리오 | 오늘의 추천 | 결과 |
|---|---|---|
| 핸드드립 + 산뜻하고 상큼한 + 신맛무관 + 상관없음 | **산미있는 원두 500g 3종** (34,000원) | ✅ 자연스러움, URL·이미지 정상 |
| 핸드드립 + 고소하고 묵직한 + 신맛회피 + 상관없음 | **고소한 원두 500g 3종** (24,000원) | ✅ 자연스러움 |
| 선물 + 잘모르겠어요→부드러움 + 없음 + 상관없음 | **선물세트 드립백·티백 14개입** (21,800원) | ✅ 자연스러움 |
| 캡슐 + 균형 + 없음 + 디카페인 | **디카페인 커피 컬렉션 5종** (10,900원) | ✅ "처음이라 부담되시면" 3번째 카드까지 정상 노출 |
| **아메리카노간편 + 고소하고 묵직한 + 신맛회피 + 상관없음** | **드립백·티백 시음용 샘플** (3,000원) | ❌ 아래 참고 |

---

## 막힌 점 / 확인된 버그

### 🔴 샘플 상품이 "오늘의 추천" 자리를 차지하는 문제 (확인됨, 재현됨)

마지막 시나리오에서 **오늘의 추천도 3,000원 시음샘플, 새로운 커피 경험도 4,000원 무료체험**이 나왔습니다. 둘 다 진짜 구매할 상품이 아니라 체험용이라, 이 흐름에서는 고객이 아무것도 제대로 못 사고 끝납니다. 발표든 실사용이든 이게 가장 중요한 장면인데 여기서 깨집니다.

**원인**: `app.js` 의 점수 계산에서 `taste === ["다양"]` 상품은 +1점, 여기에 `priority * 0.5` 를 더하는데, 샘플 상품들이 대부분 `priority: 5` 로 설정되어 있어 일반 묶음 상품과 점수가 같아집니다. 동점일 때 **가격 낮은 순**으로 정렬하기 때문에, 3,000~4,000원짜리 샘플이 정가 9,900원짜리 정식 상품을 이겨버립니다.

**고쳐야 할 곳**: `02_원두추천/app.js` 의 `baseCandidates()` 또는 `sortByScore()` 근처. **"오늘의 추천"과 "새로운 커피 경험" 후보군에서는 `sample === true` 인 상품을 아예 제외**하고, 샘플은 지금처럼 별도 3번째 칸에서만 보여주는 게 맞습니다. (원래 의도가 그랬는데 구현에서 빠졌음)

### 🟡 사소한 문구 어색함
"부드럽고 편안한 커피를 원하셔서. 선물세트 드립백·티백 14개입을 골랐어요." — 마침표 뒤에 문장이 바로 붙어 어색함. `buildMainReason` 함수의 q5 분기 문장 조립 부분 확인 필요.

### 🟡 진행 중인 작업 발견됨 (다른 세션 추정)
`data/tags.json` 에 `flavors` 필드(상품 안의 맛 옵션 목록, 예: 조선호랑이/호랑이형님/역삼동호랑이/디카페인호랑이)가 일부 상품에 추가되어 있고, `tools/build_products.py` 도 이를 `products.js` 에 반영하도록 이미 고쳐져 있습니다. **하지만:**
- `products.js` 는 아직 이 변경 전 버전 (재실행 안 됨)
- `app.js`, `index.html` 에는 `flavors` 를 화면에 보여주는 코드가 아직 없음
- 즉 **미완성 상태로 중단된 작업**입니다. 위 "다른 세션" 항목 참고 — 이어서 할지, 이 세션에서 마무리할지 판단 필요

---

## 남은 일

1. **샘플 상품 버그 수정** ← 가장 급함, 위 내용대로 `app.js` 몇 줄만 고치면 됨
2. `flavors` 기능 마무리 여부 결정 → 마무리한다면 `build_products.py` 재실행 + `app.js`/`index.html` 에 맛 선택 UI 추가
3. `git add -A` / 커밋 — **지금 이 작업 전체가 아직 커밋 안 된 상태** (`git status` 에 60개 항목)
4. Vercel에 `02_원두추천` 배포 (아직 로컬에만 있음)
5. Supabase 상담 데이터 저장 (설계서 6단계, 아직 미착수)
6. 태그 분류표(`data/tags.json`) 검토 — 51개 상품 분류는 내가 상품명만 보고 판단한 초안. 실제 상담 기준과 맞는지 확인 필요

---

## 다음 첫 단계

**`02_원두추천/app.js` 에서 샘플 상품이 "오늘의 추천"/"새로운 커피 경험" 후보에서 빠지도록 고치고, 위 5개 시나리오를 브라우저에서 다시 확인한다.**

그 다음 `flavors` 미완성 작업을 이어갈지 확인하고, 문제없으면 git 커밋 → Vercel 배포로 진행.

---

## 참고 — 실행 방법

```
python -m http.server 5500 --directory 02_원두추천
```
→ http://localhost:5500

CSV를 새로 받아서 상품 정보를 갱신하려면:
```
python 02_원두추천\tools\build_products.py
python 02_원두추천\tools\fetch_images.py
```
(단, `build_products.py` 안의 CSV 경로가 고정되어 있는지 확인 — 새 CSV 파일명이 다르면 경로를 맞춰줘야 함)

# HANDOFF — 범표원두 원두추천 프로젝트

마지막 업데이트: 2026-09-06
작업 폴더: `C:\Users\bebeb\OneDrive - 범표원두\★.김현정\claude_2026`
git 브랜치: `update-settings` (작업 트리 깨끗함, 마지막 커밋 `fa4e567`)
실서비스: **https://beompyo-wondu-chucheon.vercel.app**

---

## 0. 시작 전 체크리스트

- [ ] **같은 폴더를 다른 Claude 세션이 열고 있는지 확인.** 9/5에 두 세션이 `tools/build_products.py`를 동시에 고쳐서 한쪽이 덮어써진 사고가 실제로 있었음. 포트 5500에 다른 세션의 로컬 서버가 떠 있으면 그 신호. 검증용 서버는 **5501 이상** 포트로 띄울 것.
- [ ] `git status` 로 작업 트리가 깨끗한지 확인 (지금은 깨끗함).
- [ ] 설계 문서(`02_원두추천/docs/`)는 1일차 아이디어 문서라 "가상 제품 30종" 기준. **이 HANDOFF.md 가 최신 기준.** 설계서에 나오는 "조선호랑이 드립백" 같은 단독 상품은 실제로 없음 — 실제 스토어는 여러 맛을 **옵션**으로 묶어 팜.

---

## 1. 이 프로젝트가 뭐냐면

고객이 채팅처럼 5~6개 질문에 답하면 **범표원두 스마트스토어에 실제로 있는 상품**을 하나 추천하고, 왜 추천했는지 쉬운 말로 설명하고, 비슷한 상황의 실제 구매자 후기를 보여준 뒤 구매 페이지로 연결하는 웹서비스.

- 처음엔 내일바꿈 양양 행사 발표용 → 9/5에 **"고객이 실제로 써서 구매까지 이어지는 것"** 으로 방향 전환 (사용자 지시)
- **규칙 기반**(선택지 클릭). AI 대화 아님. 이유: 오프라인에서도 동작, 없는 상품을 지어낼 위험 없음
- 순수 HTML/CSS/JS, 외부 라이브러리 없음, `index.html` 더블클릭으로도 동작
- 톤: 짙은 브라운 `#2c1e14` 배경 / 크림 `#f3e3d0` 글자 / 크레마 브라운 `#c98a4b` 포인트

---

## 2. 지금까지 한 일 (시간순)

### 9/3~9/4 — 인사 페이지 + 프로토타입 v1
- `01_인사페이지/` 완료·배포 → https://kimhyunjeong-yangyang.vercel.app (Vercel 프로젝트 `kimhyunjeong-yangyang`)
- `02_원두추천/` v1: 가상 제품 30개로 대화 흐름·채팅 UI·추천 로직 검증

### 9/5 — 실제 판매 상품 51개로 재구축
- 스마트스토어 상품 CSV → 판매중·전시중 커피 상품 51개 연동. `data/tags.json`에 상품별 분류 직접 판단해서 기록
- 드립백 4종 세트에 **맛 선택 칩**(조선호랑이/호랑이형님/역삼동호랑이/디카페인호랑이). 이미지 1장 + 그 안에서 맛 선택. 고객 답변과 맞는 맛이 기본 선택
- **버그 수정**: 3,000원 시음샘플이 가격순 동점 처리 때문에 "오늘의 추천"을 차지하던 문제. `baseCandidates()`에서 `sample:true` 제외
- **대화감**: 답변마다 시스템이 맞장구(`ack`) 한 마디 → 다음 질문
- 첫 질문을 "원두를 사서 직접 내려요 (핸드드립, 모카포트 등)"로 넓힘 (모카포트 사용자가 고를 게 없었음)
- **Vercel 배포**: 팀 `bumpyobeans`, 프로젝트 `beompyo-wondu-chucheon`. Vercel 프로젝트 이름은 영문만 가능해서 "범표원두 추천"을 로마자로 옮긴 것
- `.claude/settings.local.json`에 `Bash(vercel *)` 허용 규칙 추가. CLAUDE.md의 "외부 작업은 확인" 규칙에서 **이 명령만 예외**로 두기로 사용자가 명시적으로 결정 (자동모드 분류기가 배포를 계속 막아서)

### 9/6 — 구매 전환 강화
- **원두 용량 질문** `q_amount`: "원두 직접 내려요"를 고른 사람에게만. 100g(처음/맛보기) / 200g(혼자·신선) / 500g(2~3명) / 1kg(넉넉히). 상품명에서 g/kg를 정규식으로 읽어(`extractWeightG`) 맞는 용량이 있으면 그것으로 후보를 좁힘. 사용자가 준 도메인 지식: 200g은 1인 1주일, 500g은 2~3인 1주일, 1kg 수요가 커피값 상승으로 늘고 있음
- **실제 후기 연동**: 사용자가 스마트스토어센터 리뷰 엑셀(2,032건, 전부 포토리뷰) 제공 → `tools/build_reviews.py` → `reviews.js`. 4점 이상·전시상태 정상만, 텍스트에서 상황 키워드를 찾아 태깅, 상품당 최대 8건. 사진은 네이버 공개 CDN 원본 URL을 그대로 hotlink (HTTP 200 확인, 다운로드 안 함)
- 결과 카드에 후기 1건 표시. 태그가 겹치면 "나와 비슷한 분의 후기", 아니면 "실제 구매자 후기". 사진 안 예쁘다는 피드백 → **60×60 썸네일**로 축소
- **카드 재배치**: 추천 이유에 "이렇게 추천했어요"/"이런 점이 새로워요" 라벨 + 강조 박스. 순서를 **이유 → 맛옵션 → 후기 → 구매 버튼** 으로 (읽고·확인하고·클릭)

모두 브라우저에서 시나리오를 끝까지 돌려 확인, 콘솔 에러 0, 배포·커밋 완료.

---

## 3. 코드가 어떻게 돌아가나 (app.js)

### 질문 흐름
```
q1(음용방식/상황) → q_amount(q1이 "핸드드립"일 때만) → q2(맛) → q3(부담 맛) → q4(카페인) → q5(q2가 "모름"일 때만) → 결과
```
`FLOW` 배열 순서대로 진행, 조건부 건너뛰기는 `nextStep()`에 있음. 각 선택지는 `label`(버튼 글자) / `value`(내부 값) / `phrase`(이유 문장용) / `ack`(맞장구) 4개 필드.

### 추천 파이프라인 (`recommend()`)
1. `baseCandidates()` — `exclude`·`sample`·품절 제외
2. `filterByQ1` — q1 값으로 `method` 또는 `scene` 필터 (`FILTER_BY_Q1` 표)
3. `filterByAmount` — 용량 일치 상품이 있으면 그것만, 없으면 그대로
4. `filterByDecaf` — q4가 "디카페인"이면 `decaf:true`만
5. `filterByAcidAvoid` — q3가 "신맛회피"면 산뜻한산미/과실감 제외 (`taste:["다양"]`은 남김)
6. `sortByScore` 1등 = **오늘의 추천**. 점수 = 맛 일치 +3 (또는 "다양" +1) + 쓴맛회피 보정 +1 + `priority×0.5`. 동점 → priority 높은 순 → 가격 낮은 순
7. **새로운 커피 경험** = 남은 후보 중 ① 제품군(`line`)이 다른 것 → ② 맛 구성이 다른 것 → ③ 그냥 2등. 후보가 없으면 필터를 단계적으로 풀어서 반드시 하나 찾음
8. **처음이라 부담되시면** = `sample:true` 중 q1 통과하는 것의 priority 1등 (위 둘과 겹치면 생략)
9. 후보가 0개면 `fallback=true` → 전체 priority 1등 + 안내 문구

### 후기 매칭 (`pickReview()`)
고객 답변을 태그로 바꾼 뒤(`currentSituationTags`: q1값, 목표맛, 디카페인, 용량), 그 상품의 후기 중 겹치는 태그가 가장 많은 것 1건. 겹침 0이어도 첫 후기를 보여줌(사회적 증거는 있는 게 낫다는 판단). 후기 없는 상품은 카드 자체 생략.

### 태그 어휘 — 문자열이 정확히 같아야 매칭됨
| 축 | 값 | 어디서 쓰나 |
|---|---|---|
| q1 | `핸드드립` `아메리카노간편` `라떼` `캡슐` `콜드브루` `선물` `여행캠핑` | tags.json `method`/`scene`, 리뷰 태그 |
| 맛 | `고소` `균형` `산뜻한산미` `과실감` `다양` | tags.json `taste`, 리뷰 태그, `TASTE_LABEL` |
| 카페인 | `디카페인` | 리뷰 태그 (상품은 `decaf:true/false`) |
| 용량 | `"100"` `"200"` `"500"` `"1000"` (문자열, 그램) | q_amount value, 리뷰 태그 |

리뷰 태그를 만드는 키워드 사전은 `tools/build_reviews.py`의 `TAG_KEYWORDS`. 새 태그를 추가하면 **양쪽(app.js 답변값 ↔ TAG_KEYWORDS 키)** 을 같이 맞춰야 함.

---

## 4. 파일 구조 (02_원두추천/)

| 파일 | 역할 | 직접 수정? |
|---|---|---|
| `index.html` | 화면 + CSS 전체 (`<style>` 안) | O |
| `app.js` | 질문·추천·화면 그리기 전부 (약 850줄, IIFE 하나) | O |
| `data/tags.json` | 상품 분류표. **사람이 관리하는 유일한 데이터** | O |
| `products.js` | 상품 51개 (`var PRODUCTS`) | **X** → `python tools\build_products.py` |
| `reviews.js` | 후기 273건 (`var REVIEWS`, 상품번호 키) | **X** → `python tools\build_reviews.py` |
| `tools/build_products.py` | 상품 CSV + tags.json → products.js | O (`CSV_PATH` 고정) |
| `tools/build_reviews.py` | 리뷰 엑셀 → reviews.js (`openpyxl` 필요) | O (`XLSX_PATH` 고정) |
| `tools/fetch_images.py` | 대표이미지 51장 → `images/p_{상품번호}.jpg` | O |
| `.vercel/` | Vercel 연결 정보 (gitignore됨) | X |

원본 데이터는 `C:\Users\bebeb\Desktop\Product_20260905_150247.csv`, `review_20260906_003018.xlsx` — 개인 파일이라 git에 없음. 새로 받으면 스크립트 상단 경로를 바꿔야 함.

---

## 5. 데이터 현황 (알고 있어야 할 숫자)

- 상품 51개: 원두 11 / 캔커피 10 / 파우더 6 / 콜드브루 6 / 선물세트 6 / 드립백 5 / 캡슐 4 / 티백 3. `업소용 3kg·5kg` 1개는 `exclude:true`
- 후기 273건이 40개 상품에 붙어 있음. **후기 없는 상품 11개**:
  - 선물세트 **5개 전부** (`9197759366` `10800170496` `13040457289` `9200019610` `13727847144`) → **"선물" 시나리오는 현재 후기 카드가 절대 안 뜸**
  - 디카페인 계열 4개 (`13623229799` 원두, `12738150675` 컬렉션, `6691740369` `6692560973` 캔커피)
  - `12972626274` 커피티백 취향 4종 맛보기, `13623204620` 밸런스 원두 500g 2종
- 후기 태그 분포: 라떼 104, 고소 94, 아메리카노간편 52, 디카페인 48, 산뜻한산미 45, 캡슐 40, 선물 37, 핸드드립 30, 콜드브루 22, 200g 19, 균형 18, 500g 17, 여행캠핑 15, 1kg 11, 100g 10, **과실감 4** (거의 없음). 태그 0개인 후기 28건
- 원두 라인 구조: 취향별 원두(`고소한/밸런스/산미있는/디카페인 원두`)는 **500g만** 있음. 100g/200g/1kg은 `9종` 옵션 상품만 있음 → 200g을 고르면 반드시 "9종 중 골라 드세요" 상품이 나오는 게 정상 동작

---

## 6. 회귀 확인용 시나리오 (직접 확인한 결과)

| q1 | q_amount | q2 | q3 | q4 | 오늘의 추천 | 후기 |
|---|---|---|---|---|---|---|
| 아메리카노간편 | — | 고소 | 신맛회피 | 상관없음 | **드립백 커피 4종 7개입** 9,900 (맛 칩 4개, 조선호랑이 기본선택) | 비슷한 분 ✅ |
| 핸드드립 | 200g | 고소 | 없음 | 상관없음 | **당일로스팅 원두 200g 9종** 12,000 | — |
| 핸드드립 | (질문 추가 전) | 고소 | 없음 | 상관없음 | **고소한 원두 500g 3종** 24,000 | — |
| 핸드드립 | 200g | 고소 | 없음 | 상관없음 | 새로운 경험 = 볼리비아 로스 로드리게스 200g | — |

기능을 고친 뒤에는 최소 1행(아메리카노간편)과 2행(핸드드립+200g)은 다시 돌려볼 것. 첫 번째는 맛 칩·후기·버튼 순서를, 두 번째는 조건부 질문과 용량 필터를 한 번에 확인함.

---

## 7. 막힌 점 / 주의사항

1. **스마트스토어 우측 배너 미완료 (사용자 직접 작업).** 스토어 PC 화면 우측에 이 서비스 링크 배너를 달고 싶어함. 판매자센터 "노출관리 → 스마트스토어 → PC → 배너/우측배너 컴포넌트"에 이미지+링크 URL 넣는 방식이라고 글로만 안내함. 정확한 메뉴명은 못 확인. 배너 이미지(세로로 좁은 형태, 폭 170px대) 제작을 요청받을 수 있음.
2. **Claude in Chrome 확장 미연결.** 판매자센터처럼 로그인 필요한 화면을 같이 보려면 설치·로그인 필요: https://chromewebstore.google.com/detail/fcoeoabgfenejglbffodgkkbkcdhcgfn . 로그인/비밀번호는 절대 대신 입력하지 않음.
3. **브라우저 팬에서 `*.vercel.app` 열람이 정책상 막힘.** 배포 확인은 CLI 응답(`readyState: READY`)까지만. 화면 확인은 로컬 서버로.
4. **후기 사진 품질 들쭉날쭉.** 썸네일로 축소해 눈에 덜 띄게 했지만 사진을 고르는 기준은 없음. 필요하면 `build_reviews.py`에 제외할 리뷰글번호 목록을 두는 방식이 현실적.
5. **선물 시나리오에 후기가 없음** (위 5절). 선물세트 후기가 생기면 엑셀 다시 받아 `build_reviews.py` 재실행.
6. `TAG_KEYWORDS`와 `data/tags.json`은 둘 다 내가 샘플/상품명만 보고 만든 초안. 어색한 매칭이 보이면 값을 고치고 스크립트 재실행.
7. 브라우저 자동화에서 `ref` 클릭이 페이지 새로고침 직후 가끔 안 먹음 → 스크린샷 찍고 좌표로 클릭하면 됨.
8. git CRLF 경고는 무해함.

---

## 8. 하지 말 것

- `products.js`, `reviews.js` 직접 편집 (재생성 시 날아감)
- 포트 5500 사용 (다른 세션 충돌)
- 저장소 루트에서 `vercel` 실행 — **반드시 `cd 02_원두추천` 후** (`.vercel/` 링크가 그 안에 있음)
- 후기·상품을 지어내기. 없으면 없는 대로 생략하는 게 이 프로젝트의 원칙
- `.env` 수정 (CLAUDE.md 절대 규칙)

---

## 9. 사용자와 일하는 방식 (이 세션에서 확인된 것)

- 답은 **한국어**, 짧게, 핵심부터. 전문용어는 풀어서
- 기능 하나 만들기 전에 "무엇을 만들지" 한 줄로 먼저 말하고, 만든 뒤 **브라우저에서 직접 확인**한 후에만 "완료"
- 배포·커밋은 **매번 사용자가 "배포하고 커밋해줘"라고 말할 때만**. 미리 하지 않음
- 선택지 질문(AskUserQuestion)을 자주 닫고 채팅으로 직접 답함 → 질문은 정말 필요할 때 1개만
- 실제 매장 운영 감각이 있음(용량별 구매 목적, 모카포트 사용자, 후기의 설득력). 도메인 판단은 사용자 말을 그대로 반영하고 기술 판단만 내가 함

---

## 10. 남은 일

1. 스마트스토어 우측 배너 이미지 제작 + 연결 (7-1)
2. 실서비스에서 여러 시나리오를 돌려 후기 매칭·문구 어색한 곳 찾아 고치기
3. Supabase 상담 데이터 저장 (설계서 6단계, 가입만 됨·프로젝트 안 만듦)
4. `data/tags.json` 분류 검토 (실제 상담 기준과 맞는지)
5. `01_인사페이지/ocean.html` 최종 확인 (급하지 않음)

---

## 11. 다음 첫 단계

**https://beompyo-wondu-chucheon.vercel.app 를 6절 시나리오 포함 5~6가지로 직접 눌러보고, 후기 매칭이 어색하거나 문구가 이상한 곳을 찾아 고친다.** 특히 "선물" 시나리오(후기 없음)와 "핸드드립 + 산뜻 + 100g"(과실감 태그가 거의 없어 매칭이 약할 것) 두 가지를 먼저 볼 것. 그 다음 스마트스토어 배너 작업으로.

---

## 12. 실행/재생성 방법

로컬 확인:
```
python -m http.server 5501 --directory 02_원두추천
```
→ http://localhost:5501

데이터 갱신 (저장소 루트에서):
```
python 02_원두추천\tools\build_products.py    # 상품 CSV + tags.json → products.js
python 02_원두추천\tools\fetch_images.py       # 대표이미지
python 02_원두추천\tools\build_reviews.py     # 리뷰 엑셀 → reviews.js
```

배포:
```
cd 02_원두추천
vercel --prod --yes
```
계정 `bebebek1005-6477`, 팀 `bumpyobeans`. 끝에 `readyState: READY` 가 나오면 성공.

커밋:
```
git add <바뀐 파일들>
git commit -m "..."
```
(커밋 메시지 끝에 `Co-Authored-By: Claude ... <noreply@anthropic.com>` 붙이는 관례)

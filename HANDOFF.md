# HANDOFF — 범표원두 원두추천 프로젝트

마지막 업데이트: 2026-09-04 (행사 2일차)
작업 폴더: `C:\Users\bebeb\OneDrive - 범표원두\★.김현정\claude_2026`

---

## 이 프로젝트가 뭐냐면

내일바꿈 양양 행사 **4일차 발표용**으로 만드는 "범표원두 커피 추천 상담" 웹서비스.
고객이 몇 가지 질문에 답하면 **범표원두가 실제로 파는 제품 중에서** 하나를 추천하고,
왜 추천했는지 쉬운 말로 설명하고, 새로운 커피 경험까지 제안한다.

설계 문서는 `02_원두추천/docs/` 안에 있다. **작업 시작 전에 반드시 읽을 것.**
- `01_설계.md` — 무엇을 왜 만드는지 (제품 목록, 추천 원칙, 발표 시나리오)
- `02_계획.md` — 단계별 작업 순서와 완료 기준

---

## 지금까지 한 일

### 환경 준비 (1일차, 9/3)
- Node.js v24.16.0 / npm 11.13.0 확인 (설치되어 있었음)
- Vercel 로그인 완료 — 계정 `bebebek1005-6477`, 팀 `bumpyobeans`
- Supabase 가입 완료 (GitHub 계정으로 로그인). **프로젝트는 아직 안 만들었음**
- Vercel 플러그인 0.44.0 → 0.48.0 업데이트

### 01_인사페이지 (1일차 미션, 완료)
- 인사 페이지 2종 제작: `index.html`(커피 컨셉), `ocean.html`(양양 바다 픽셀아트)
- Vercel 실배포 완료 → **https://kimhyunjeong-yangyang.vercel.app**
  - 바다 버전은 `/ocean.html`
  - Vercel 프로젝트명 `kimhyunjeong-yangyang`
- ⚠️ `ocean.html`은 파도 모양을 다듬던 중 미리보기가 멈춰서 **최종 확인을 못 했다.** 지금 상태로도 배포는 되어 있음

### 폴더 구조 정리 (2일차)
같은 이름 폴더가 두 군데 있어서 헷갈렸던 문제를 해결했다.

```
claude_2026\
├─ CLAUDE.md          ← 작업 규칙
├─ HANDOFF.md         ← 이 파일
├─ .gitignore         ← 교육자료 PDF(32MB)와 .vercel 제외
├─ .claude\launch.json ← 로컬 서버 설정 (python http.server, 포트 5500)
├─ 00_교육자료\        ← PDF 6개
├─ 01_인사페이지\      ← 완료·배포됨
└─ 02_원두추천\        ← 지금 만드는 것
    ├─ index.html
    ├─ products.js
    ├─ app.js
    └─ docs\01_설계.md, 02_계획.md
```

### 02_원두추천 — 상담 프로토타입 1차 (2일차, 동작 확인됨)

**중요한 결정: 규칙 기반(선택지 클릭) 방식으로 간다.** AI 대화 방식이 아니다.
이유 — 발표 때 인터넷이 끊겨도 작동해야 하고(계획서 8단계 조건), AI가 범표원두에 없는
제품을 지어낼 위험이 없어야 하며(설계서 5절), 남은 시간이 부족하다.
대신 화면은 채팅처럼 보이게 만들어서 발표에서 보이는 그림은 거의 같다.

만든 파일 3개 (전부 순수 HTML/CSS/JS, 외부 라이브러리 없음, 더블클릭으로 열림):

| 파일 | 내용 |
|---|---|
| `index.html` | 첫 화면 + 채팅 UI + 디자인(짙은 브라운/크림색) |
| `products.js` | 제품 30종 데이터 (원두 9, 드립백 4, 티백 4, 캡슐 6, 파우더 5, 콜드브루 2) |
| `app.js` | 질문 흐름 + 추천 로직 + 화면 그리기 |

**질문 흐름:** 음용 방식 → 좋아하는 맛 → 부담스러운 맛 → 카페인 → (맛을 "잘 모르겠어요"로 답한 경우에만) 추가 질문 1개

**브라우저에서 직접 눌러 확인한 것:**
- 첫 화면 → 상담 → 추천 카드까지 끊김 없음, 콘솔 오류 0건
- 설계서 11절 시나리오 재현됨: 아메리카노 간편 + 고소 + 신맛회피 → **조선호랑이 드립백** ✅
- 추천 이유 문장에 고객이 고른 답이 실제로 들어감
- 디카페인 필터, 조건부 질문, 다시하기 정상
- 추천 제품과 새로운 경험 제품이 겹치지 않음

---

## 남은 일

계획서(`docs/02_계획.md`) 기준으로 남은 것:

1. **제품 이미지 + 상품 URL 수집** ← 가장 급함
   `products.js`의 `image`, `url` 필드가 30개 전부 빈 문자열이다.
   계획서 7단계("실제 범표원두 제품으로 연결")와 발표의 핵심 장면이 여기에 걸려 있다.
   네이버 스마트스토어에서 제품별 이미지 주소와 상품 페이지 주소를 가져와야 한다.
2. **Supabase 상담 데이터 저장** (계획서 6단계)
   Supabase 프로젝트 생성 → 테이블 생성(`sessions`, `answers`, `recommendations`) → 연결
3. **추천 결과 화면 다듬기** (계획서 5단계) — 이미지가 들어오면 같이 정리
4. **Vercel 배포** — 02_원두추천을 별도 Vercel 프로젝트로 올리기
5. **발표 리허설** (계획서 8단계) — 대표 시나리오 고정하고 반복 테스트

---

## 막힌 점 / 알려진 문제

1. **제품 이미지와 상품 URL이 없다.** 이게 없으면 발표의 핵심 장면("실제 제품이 화면에 등장")이 안 된다. 지금은 커피잔 이모지 자리표시자와 "상품 링크 준비 중" 비활성 버튼으로 대체 중.

2. **추천 이유 문구에 오류 하나.** 맛을 "잘 모르겠어요"로 답하고 "부드럽고 편안한 커피"를 고른 경우, 추천 이유에 *"평소 균형잡힌 잔잔한 맛을 좋아하시니"* 라고 나온다. 고객이 그렇게 말한 적이 없다. 내부적으로 변환한 값이 문장에 새어나온 것. `app.js`의 새로운 경험 제품 설명 문장 조립 부분을 고쳐야 한다.

3. **`ocean.html` 최종 확인 미완료.** 파도 모양을 다듬다가 미리보기가 멈췄다. 급하지 않음.

4. **Supabase 프로젝트가 아직 없다.** 가입만 되어 있는 상태.

---

## 다음 첫 단계

**`products.js`의 30개 제품에 이미지 주소와 상품 URL 채워넣기.**

진행 방법 두 가지 중 하나를 고르면 된다.
- (a) 김현정님이 네이버 스마트스토어에서 제품별 상품 URL을 모아서 알려주면 그걸 넣는다
- (b) 스마트스토어 주소를 알려주면 내가 페이지를 열어서 제품명·이미지·URL을 찾아 정리한다

먼저 **발표 시나리오에 나오는 제품 2개(조선호랑이 드립백, 호랑이형님 드립백)부터** 채우면
데모 화면이 바로 완성된다. 나머지는 그 다음에 채워도 된다.

---

## 참고 — 실행 방법

로컬에서 화면 확인:
```
claude_2026\02_원두추천\index.html  ← 더블클릭
```

또는 로컬 서버로 (JS 동작 확인용):
```
python -m http.server 5500 --directory 02_원두추천
```
→ http://localhost:5500

배포된 인사 페이지: https://kimhyunjeong-yangyang.vercel.app
